@model List<List<QBCS.Service.ViewModel.RuleViewModel>>
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
    
}

<h2>Rules</h2>
@foreach (List<QBCS.Service.ViewModel.RuleViewModel> rvm in Model)
{
    <div class="card shadow mb-4">
        <a href="#collapse_@rvm.FirstOrDefault().GroupTypeEnum" class="d-block card-header py-3" data-toggle="collapse" role="button" aria-expanded="true" aria-controls="collapseCardExample">
            <h6 class="m-0 font-weight-bold text-primary">@rvm.FirstOrDefault().GroupTypeEnum Rule</h6>
        </a>
        <div class="card-body collapse show" id="collapse_@rvm.FirstOrDefault().GroupTypeEnum" style="padding-left:30px">
            <button class="btn btn-primary" onclick="addField(event,'table_@rvm.FirstOrDefault().GroupTypeEnum','@rvm.FirstOrDefault().Value.FirstOrDefault().')">Add @rvm.FirstOrDefault().GroupTypeEnum Banned Word</button>
            <div class="table-responsive">
                <table class="table table-bordered" @*id="dataTable"*@ id="table_@rvm.FirstOrDefault().GroupTypeEnum" cellspacing="0">
                    <thead>
                        <tr>
                            <th>Code</th>
                            <th>Name</th>
                            <th class="col-sm">Value</th>
                            <th class="col-sm">Activate Date</th>
                            @*<th>Group</th>*@
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var rule in rvm)
                        {
                            foreach (var value in rule.Value)
                            {
                                <tr>
                                    <td hidden>@value.KeyId</td>
                                    <td>@rule.Code</td>
                                    <td>@rule.Name</td>
                                    <td><input type="text" name="@value.Value" value="@value.Value" placeholder="@value.Value" /></td>
                                    <td><input type="datetime-local" /></td>
                                    @*<td>@value.ValueGroup</td>*@
                                </tr>
                            }

                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
}


@*<div class="card shadow mb-4">
    <a href="#collapseOption" class="d-block card-header py-3" data-toggle="collapse" role="button" aria-expanded="true" aria-controls="collapseCardExample">
        <h6 class="m-0 font-weight-bold text-primary">Option Rule</h6>
    </a>
    <div class="card-body collapse show" id="collapseOption" style="padding-left:30px">
        <button class="btn btn-primary">Add Option Rule</button>
        <div class="table-responsive">
            <table class="table table-bordered" id="datatableOption" cellspacing="0">
                <thead>
                    <tr>
                        <th>Code</th>
                        <th>Name</th>
                        <th class="col-md-6">Value</th>
                        <th>Create Date</th>
                        <th>Activate Date</th>
                        <th>Group</th>
                    </tr>
                </thead>

                <tbody>
                    <tr>
                        <td>Test</td>
                        <td>TestName</td>
                        <td><input type="text" style="width:100%" /></td>
                        <td><input type="date" style="width:150px" /></td>
                        <td><input type="date" style="width:150px" /></td>
                        <td><input type="text" style="width:50px" /></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>*@
<div style="text-align:center"><button class="btn-lg btn-primary">Save Rule</button></div>
<script>
    function addField(event,tableId) {
        event.preventDefault();
        var table = document.getElementById(tableId);
        if (tableId.includes('Question')) {
            var row = table.insertRow(0);
            var cell0 = row.insertCell(0);
            var cell1 = row.insertCell(1);
            var cell2 = row.insertCell(2);
            var cell3 = row.insertCell(3);
            var cell4 = row.insertCell(4);
            cell0.appendChild(document.createElement("input"));
        }
        table.append('<tr>' +
            '<td><input type="text" />' +
            '<td><input type="text" />' +
            '<td><input type="text" />' +
            '<td><input type="text" />' +
            '<td><input type="text" />' +
            '</tr>');
    }
</script>

