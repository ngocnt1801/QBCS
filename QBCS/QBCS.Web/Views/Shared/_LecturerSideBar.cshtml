@using QBCS.Service.ViewModel

@{ 
    var homeClass = "";
    var courseClass = "";
    var questionClass = "";
    var userClass = "";
    var controller = this.ViewContext.RouteData.Values["controller"].ToString();
    switch (controller)
    {
        case "Home":
            homeClass = "active";
            break;
        case "Course":
            courseClass = "active";
            break;
        case "Question":
            questionClass = "active";
            break;
        case "User":
            userClass = "active";
            break;
    }
    }

<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar"
    onclick="nav_bar_active()">

    <!-- Sidebar - Brand -->
    <a class="sidebar-brand d-flex align-items-center justify-content-center" href="index.html">
        @*<div class="sidebar-brand-icon rotate-n-15">
            <i class="fas fa-laugh-wink"></i>
        </div>*@
        <div class="sidebar-brand-text mx-3">Lecturer</div>
    </a>
    
    <!-- Divider -->
    <hr class="sidebar-divider my-0">

    <li class="nav-item @homeClass">
        <a class="nav-link" href="@Url.Action("Index", "Home",new { id = ((UserViewModel)Session["user"]).Id })">
            <i class="fas fa-fw fa-tachometer-alt"></i>
            <span>Dashboard</span>
        </a>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider">

    <!-- Heading -->
    <!-- Nav Item - Courses Menu -->
    <li class="nav-item @courseClass">
        <a class="nav-link" href="@Url.Action("Index", "Course",new { userId = ((UserViewModel)Session["user"]).Id })">
            <i class="fas fa-book"></i>
            <span>Courses</span>
        </a>
    </li>

    <!-- Nav Item - Question Menu -->
    <li class="nav-item @questionClass">
        <a class="nav-link" 
           href="@Url.Action("Index", "Question")">
            <i class="fas fa-question"></i>
            <span>Questions</span>
        </a>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider">

    <!-- Heading -->
    <!-- Nav Item - Account -->
    <li class="nav-item @userClass">
        <a class="nav-link" 
           href="@Url.Action("Details", "User",new { userId = ((UserViewModel)Session["user"]).Id })">
            <i class="fas fa-user fa-chart-area"></i>
            <span>Profile</span>
        </a>
    </li>




    <!-- Divider -->
    <hr class="sidebar-divider d-none d-md-block">

    <!-- Sidebar Toggler (Sidebar) -->
    <div class="text-center d-none d-md-inline">
        <button class="rounded-circle border-0" id="sidebarToggle"></button>
    </div>

</ul>
